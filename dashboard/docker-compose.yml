version: "3.3"

x-arcane:
  icon: https://cdn.jsdelivr.net/gh/selfhst/icons@main/svg/glance.svg
  urls:
    - https://github.com/glanceapp/glance

services:
  glance:
    container_name: glance
    hostname: glance.internal
    image: glanceapp/glance
    restart: unless-stopped
    environment:
      - JELLYFIN_API_KEY
    volumes:
      - /glance/config:/app/config
      - /glance/assets:/app/assets
      - /etc/localtime:/etc/localtime:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - exposed-internal
    labels:
      # traefik
      traefik.enable: true
      traefik.docker.network: exposed-internal
      traefik.http.routers.glance.rule: Host(`glance.mnemosyne.lan`)
      traefik.http.routers.glance.entrypoints: web
      traefik.http.routers.glance.tls: true
      traefik.http.services.glance.loadbalancer.server.port: 8080

      # arcane
      com.getarcaneapp.arcane.icon: https://cdn.jsdelivr.net/gh/selfhst/icons@main/svg/glance.svg

      # glance
      glance.category: other
      glance.name: Glance
      glance.icon: di:glance
      glance.url: https://glance.mnemosyne.lan:81/
      glance.description: Pretty dashboard

networks:
  exposed-internal:
    external: true
